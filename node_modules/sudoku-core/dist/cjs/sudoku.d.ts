import { Board, Options, AnalyzeData } from "./types";
export declare function createSudokuInstance(options?: Options): {
    solveAll: () => Board;
    solveStep: ({ analyzeMode, iterationCount, }?: {
        analyzeMode?: boolean | undefined;
        iterationCount?: number | undefined;
    }) => Board | false;
    analyzeBoard: () => AnalyzeData;
    getBoard: () => Board;
    generateBoard: () => Board;
};
